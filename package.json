{
  "name": "just-t",
  "version": "0.1.0",
  "description": "Tiny i18n: JSON in → string out.",
  "keywords": [
    "i18n",
    "translation",
    "intl",
    "typescript",
    "tiny"
  ],
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "README.md", "LICENSE"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "dev": "tsx src/dev.ts",
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepack": "pnpm run clean && pnpm run build",
    "pack": "pnpm run prepack && pnpm -s pack"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "6.0.2"
  }
}
