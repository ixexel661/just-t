{
  "name": "@just-t/workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:types": "pnpm -r test:types",
    "pack": "pnpm --filter just-t pack"
  }
}
